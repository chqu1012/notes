* Letzte Zeile bestimmen: letzte_Zeile = ThisWorkbook.Sheets("DATEN").Cells(Rows.Count, 1).End(xlUp).Row
* Monat von Datum: Month(datum)
* Summe mit Condition bilden: =summewenns()
* Datum formatieren: Format(datumCombo.Value, "DD.MM.YYYY")
